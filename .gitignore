node_modules/
dist/
build/
.next/
coverage/

.env
.env.local
.env.*.local

# Apple Music API private keys — never commit (lives in ~/.gstack/secrets/)
*.p8
AuthKey_*.p8

.DS_Store
*.log
npm-debug.log*
yarn-debug.log*

.vscode/
.idea/

bun.lockb

# Nested v1 reference repo (has its own .git + contains .env with secrets)
radiomke-playlist-app V1/

# Convex / Turbo / other tooling caches
.convex/
.turbo/
.trigger/
yarn.lock

# Convex dev scaffolding landed at repo root by default; will move to packages/convex/ in Milestone 3.
# Ignore the misplaced copy until then so we don't commit it in the wrong location.
/convex/

# Trigger.dev tasks landed at repo-root /src/trigger/ via default config.
# Will relocate to services/trigger/src/ during Milestone 4.
/src/
