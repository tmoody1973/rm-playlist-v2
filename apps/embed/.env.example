# Convex public URL — baked into the embed bundle at build time.
# Partner stations never configure this; it's encoded in /v1/widget.js.
# For local dev, copy from apps/web/.env.local (NEXT_PUBLIC_CONVEX_URL) or
# from `bunx convex dev` output when you boot the Convex dev server.
VITE_CONVEX_URL=
