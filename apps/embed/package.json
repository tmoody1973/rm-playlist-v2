{
  "name": "@rm/embed",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "rm-playlist-v2 embeddable widget bundle. Preact + Vite, shipped to embed.radiomilwaukee.org via Cloudflare Pages.",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no unit tests yet in @rm/embed — Playwright suite lands with widget variants'",
    "lint": "echo 'lint covered by root eslint runs'",
    "preview": "vite preview"
  },
  "dependencies": {
    "convex": "^1.17.0",
    "preact": "^10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.10.1",
    "@types/node": "^22.10.0",
    "terser": "^5.37.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.7"
  }
}
